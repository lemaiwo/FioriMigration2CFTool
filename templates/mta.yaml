_schema-version: "3.2"
ID: {{reponame}}
description: Generated by migration tool of Wouter Lemaire
version: 0.0.1
modules:
- name: {{reponame}}-dest-content
  type: com.sap.application.content
  requires:
  - name: dest-srv
    parameters:
      content-target: true
  - name: {{reponame}}-repo-host
    parameters:
      service-key:
        name: {{reponame}}-repo-host-key
  - name: {{reponame}}-uaa
    parameters:
      service-key:
        name: {{reponame}}-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: {{reponame}}_repo_host
          ServiceInstanceName: {{reponame}}-html5-srv
          ServiceKeyName: {{reponame}}-repo-host-key
          sap.cloud.service: {{reponame}}
        - Authentication: OAuth2UserTokenExchange
          Name: {{reponame}}_uaa
          ServiceInstanceName: {{reponame}}-xsuaa-srv
          ServiceKeyName: {{reponame}}-uaa-key
          sap.cloud.service: {{reponame}}
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: {{reponame}}-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: {{reponame}}-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - {{reponame}}.zip
      name: {{reponame}}
      target-path: resources/
- name: {{reponame}}
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: dest-srv
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: dest-srv
    service-plan: lite
- name: {{reponame}}-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: {{reponame}}-xsuaa-srv
    service-plan: application
- name: {{reponame}}-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: {{reponame}}-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true